# Wallet assets

Los assets PNG del `.pkpass` se generan en runtime desde `app/lib/wallet-pass-assets.ts` para evitar conflictos de merge por archivos binarios en ramas.

Si quieres reemplazarlos por branding real, actualiza el contenido base64 en ese archivo.
